# TG Parser runtime env
# NOTE: do not commit real secrets; keep real values in .env (ignored) or your secret store

# Telegram bot
BOT_TOKEN=

# Optional: operator notifications
# ADMIN_CHAT_ID=

# Postgres (SQLAlchemy URL)
# For local docker-compose: user=postgres pass=postgres host=db port=5432 db=tgparser
DATABASE_URL=postgresql+psycopg://tgparser:tgparser@db:5432/tgparser

# Optional: override Postgres container defaults
POSTGRES_DB=tgparser
POSTGRES_USER=tgparser
POSTGRES_PASSWORD=tgparser

# Redis
REDIS_URL=redis://redis:6379/0

# Parser/worker
TICK_INTERVAL_SECONDS=3600
DEFAULT_BACKFILL_DAYS=0

# HTTP API (integrations)
# All API requests require: Authorization: Bearer <SERVICE_API_TOKEN>
SERVICE_API_TOKEN=
# Port exposed on localhost in prod compose override (see INTEGRATION.md)
# TG_PARSER_API_PORT=18081

# Telethon
# API_ID/HASH are stored per-account in DB (like tgreact).
# TELETHON_API_ID/HASH are NOT used as global env settings in v1.
